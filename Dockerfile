# ──────────────────────────────────────────────────────────────────────────────
# Dockerfile — DUMMY (not needed for local development)
#
# This project runs directly without Docker. Use:
#
#   cd src
#   dotnet run
#
# The app will be available at http://localhost:5000
# Default login: admin@taskmanager.com / Admin@123
# ──────────────────────────────────────────────────────────────────────────────

# (No build instructions — not needed for local dev)
